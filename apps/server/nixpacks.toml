[phases.build]
cmds = [
  "pnpm install --frozen-lockfile",
  "pnpm --filter @memetgc/types build",
  "pnpm --filter @memetgc/db build",
  "pnpm --filter @memetgc/server build"
]

[start]
cmd = "node apps/server/dist/index.js"
